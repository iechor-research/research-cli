# Use the public npm registry for all scopes.
# (Upstream gemini-cli redirected @google/* to Google's internal
#  wombat-dressing-room publishing proxy, which is not installable.)
registry=https://registry.npmjs.org/
